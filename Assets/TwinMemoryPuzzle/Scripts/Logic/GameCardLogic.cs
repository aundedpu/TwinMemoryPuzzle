using UnityEngine;

namespace TwinMemoryPuzzle.Scripts.Logic
{
    public class GameCardLogic : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
